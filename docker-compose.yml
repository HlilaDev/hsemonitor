services:
  backend:
    build: ./backend
    container_name: hse-backend-container
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: hse-frontend-container
    ports:
      - "4200:80"      
    depends_on:
      - backend
    restart: unless-stopped